#!/usr/bin/zsh

if [ -f ~/.env ]; then
  source ~/.env
fi

FONT=HackNerdFontMono-Regular:size=

if [ "$ULTRAWIDE" ]; then
  FONT="$FONT"12
else
  FONT="$FONT"10.5
fi

foot --override include=~/dotfiles/foot/vscode.ini --font="$FONT" "$@"
