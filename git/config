[user]
  name = AkisArou
  email = akisarou90@gmail.com

[github]
	user = AkisArou

[includeIf "gitdir:~/nable-solutions/.git"]
  path = ./config-nable

[core]
  pager = delta
  # Global ignore file (not shared)
  excludesfile = ~/.gitignore_global
  editor = nvim
  # Don't consider trailing space change as a cause for merge conflicts
  whitespace = -trailing-space

[delta]
  navigate = true    # use n and N to move between diff sections
  dark = true
  features = syntax-highlighting
  syntax-theme = tokyonight
  line-numbers = true
  true-color = always
  line-numbers-left-format = "{nm:^4}⋮ "
  line-numbers-right-format = "{np:^4}: "
  hunk-header-decoration-style = "#24283b ul"
  file-style ="#ff9e64"
  file-decoration-style = "#1a1b26 ul ol" # mimic space


  # tokyonight night
  # minus
  minus-style                   = "#a85b63"
  minus-emph-style              = syntax "#4a272f"
  minus-empty-line-marker-style = "#4a272f"
  line-numbers-minus-style      = "#a85b63"
  # plus
  plus-style                    = "#9ece6a"
  plus-emph-style               = syntax "#223608"
  plus-empty-line-marker-style  = "#223608"
  line-numbers-plus-style       = "#9ece6a"
  #zero
  line-numbers-zero-style       = "#3b4261"

[interactive]
  diffFilter = delta --color-only

[merge]
	tool = codediff

[mergetool]
  prompt = false
  keepBackup = false

[mergetool "nvimdiff"]
  layout = "LOCAL,BASE,REMOTE / MERGED"

[mergetool "codediff"]
	cmd = nvim \"$MERGED\" -c \"CodeDiff merge \\\"$MERGED\\\"\"

[diff]
	tool = codediff
  colorMoved = default

[difftool]
	prompt = false

[difftool "nvimdiff"]
  cmd = "nvim -d \"$LOCAL\" \"$REMOTE\""

[difftool "nvimdifftool"]
  cmd = nvim -c "\"DiffTool $LOCAL $REMOTE\""

[difftool "codediff"]
	cmd = nvim "\"$LOCAL\" \"$REMOTE\" +\"CodeDiff $LOCAL $REMOTE\""

[fetch]
  # Auto-fetch submodule changes (sadly, won't auto-update)
  recurseSubmodules = on-demand

[grep]
  break = true
  heading = true
  lineNumber = true
  # Consider most regexes to be ERE
  extendedRegexp = true

[log]
  # Use abbrev SHAs whenever possible/relevant instead of full 40 chars
  abbrevCommit = true
  # Automatically --follow when given a single path
  follow = true
  # Disable decorate for reflog
  # (because there is no dedicated `reflog` section available)
  decorate = false
	date = format-local:%A %d/%m/%Y %H:%M

[pull]
  rebase = merges

[push]
  # Default push should only push the current branch to its push target, regardless of its remote name
  default = upstream
  # When pushing, also push tags whose commit-ishs are now reachable upstream
  followTags = true

[status]
  # Display submodule rev change summaries in status
  submoduleSummary = true
  # Recursively traverse untracked directories to display all contents
  showUntrackedFiles = all

[tag]
  # Sort tags as version numbers whenever applicable, so 1.10.2 is AFTER 1.2.0.
  sort = version:refname

[versionsort]
  prereleaseSuffix = -pre
  prereleaseSuffix = .pre
  prereleaseSuffix = -beta
  prereleaseSuffix = .beta
  prereleaseSuffix = -rc
  prereleaseSuffix = .rc

[init]
	defaultBranch = main

[credentials]
	helper = --help
